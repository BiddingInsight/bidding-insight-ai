import React from 'react';

// The image has been removed to improve performance.
export const MascotImageReading: React.FC<{ className?: string }> = ({ className }) => {
  // Returning null as the component is unused and the large image data caused performance issues.
  return null;
};
